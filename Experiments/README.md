# Experiments

Each directory in this folder contains a self-contained Kubernetes
deployment of a non-standard software stack.

To add a new experiment:

1. Create a folder under `Experiments/<Name>/`
2. Include `Container/`, `K8S/`, `Helm/` as needed
3. Document how to build and deploy it
